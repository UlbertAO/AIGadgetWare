{
  "IsEncrypted": false,
  "Values": {
    "AzureWebJobsStorage": "UseDevelopmentStorage=true",
    "FUNCTIONS_WORKER_RUNTIME": "dotnet",

    "SMTP_HOST": "YOUR_SMTP_HOST_ADDRESS", // if using gmail smtp then=> smtp.gmail.com
    "SMTP_PORT": "YOUR_SMTP_PORT", // if using gmail smtp then=> 587
    "SMTP_ADDRESS": "SENDERS_ADDRESS",
    "SMTP_USER_NAME": "SENDERS_NAME",
    "SMTP_PASSWORD": "SENDERS_PASSWORD", // if using gmail smtp then=> use APP PASSWORD instead of regular password
    "RECIPIENT_EMAILS": "COMMA_SEPERATED_RECIPIENT_EMAILS",
    "SENDGRID_API_KEY": "YOUR_SENDGRID_API_KEY"
  }
}